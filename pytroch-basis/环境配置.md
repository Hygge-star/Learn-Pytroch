# PyTorch 环境配置 - 大二初学者专属指南 🚀

> 专门为大学生设计的简单、清晰的 PyTorch 环境配置方案，避开复杂的技术细节，让你快速开始学习！

## 🎯 推荐方案：Google Colab（首选）

### 为什么推荐给初学者？
- ✅ **完全免费** - 不需要信用卡
- ✅ **无需安装** - 打开浏览器就能用
- ✅ **自带 GPU** - 免费使用 Tesla T4/V100 显卡
- ✅ **环境预配置** - PyTorch 已经安装好
- ✅ **云端保存** - 代码保存在 Google Drive
- ✅ **协作方便** - 可以和同学共享笔记本

### 使用步骤：

#### 1. 打开网站
访问 [colab.research.google.com](https://colab.research.google.com)

#### 2. 创建新笔记本
- 点击 **"新建笔记本"**
- 命名为 "我的第一个PyTorch程序"

#### 3. 验证环境
在代码单元格中输入：

```python
# 测试PyTorch环境
import torch
import torchvision

print("🎉 PyTorch 环境就绪！")
print(f"PyTorch 版本: {torch.__version__}")
print(f"CUDA 可用: {torch.cuda.is_available()}")

if torch.cuda.is_available():
    print(f"🎮 正在使用: {torch.cuda.get_device_name(0)}")
```

#### 4. 启用 GPU 加速
- 点击右上角 **"连接"**
- 点击 **"更改运行时类型"**
- 选择 **"GPU"** → **"保存"**

---

## 💻 本地安装方案

### 方案A：最简单的 Pip 安装

**适合**：想快速开始，不关心复杂配置

```bash
# 1. 打开命令提示符(Windows)或终端(Mac/Linux)

# 2. 安装 PyTorch (CPU版本，适合学习)
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu

# 3. 安装学习工具
pip install jupyter matplotlib numpy pandas

# 4. 启动 Jupyter
jupyter notebook
```

### 方案B：使用 Anaconda（推荐）

**适合**：计算机相关专业，想认真系统学习

#### 安装步骤：

1. **下载 Anaconda**
   - 访问 [anaconda.com/download](https://www.anaconda.com/download)
   - 选择你的操作系统
   - 下载 Python 3.9+ 版本

2. **安装 Anaconda**
   - 双击安装包，全部点击"下一步"
   - **重要**：勾选 "Add Anaconda to PATH"

3. **安装 PyTorch**
   ```bash
   # 打开 Anaconda Prompt (Windows) 或终端 (Mac/Linux)
   
   # 创建专门的学习环境
   conda create -n pytorch-learning python=3.9
   
   # 激活环境
   conda activate pytorch-learning
   
   # 安装 PyTorch
   conda install pytorch torchvision torchaudio -c pytorch
   
   # 安装学习工具
   conda install jupyter matplotlib numpy pandas
   ```

4. **启动学习环境**
   ```bash
   # 每次学习前运行：
   conda activate pytorch-learning
   jupyter notebook
   ```

---

## 🧪 环境验证测试

创建 `test_pytorch.py` 文件：

```python
# test_pytorch.py - 环境测试脚本

import torch

def main():
    print("=" * 50)
    print("🌟 PyTorch 学习环境测试")
    print("=" * 50)
    
    # 测试基本功能
    print("1. 测试张量创建...")
    x = torch.tensor([1, 2, 3, 4, 5])
    print(f"   创建张量: {x}")
    print(f"   张量形状: {x.shape}")
    
    print("2. 测试数学运算...")
    y = x * 2 + 1
    print(f"   数学运算: {x} * 2 + 1 = {y}")
    
    print("3. 测试GPU支持...")
    if torch.cuda.is_available():
        print("   🎮 检测到GPU！深度学习会很快！")
        device = torch.device('cuda')
        x_gpu = x.to(device)
        print(f"   GPU张量: {x_gpu}")
    else:
        print("   💻 使用CPU模式，学习基础完全足够")
    
    print("4. 测试自动微分...")
    # 创建一个需要梯度的张量
    w = torch.tensor(2.0, requires_grad=True)
    b = torch.tensor(1.0, requires_grad=True)
    
    # 简单计算：y = w*x + b
    y_pred = w * 3.0 + b  # 假设 x=3.0
    loss = (y_pred - 7.0) ** 2  # 假设真实值应该是7
    
    # 反向传播
    loss.backward()
    
    print(f"   损失值: {loss.item():.2f}")
    print(f"   w的梯度: {w.grad:.2f}")
    print(f"   b的梯度: {b.grad:.2f}")
    
    print("\n" + "=" * 50)
    print("✅ 环境测试通过！开始你的深度学习之旅吧！")
    print("=" * 50)

if __name__ == "__main__":
    main()
```

**运行方法：**
```bash
python test_pytorch.py
```

---

## 🛠️ 开发工具配置

### 代码编辑器推荐

#### 1. VS Code（推荐）
- **下载**: [code.visualstudio.com](https://code.visualstudio.com)
- **安装扩展**:
  - Python
  - Jupyter
  - Pylance

#### 2. PyCharm Community
- **下载**: [jetbrains.com/pycharm](https://www.jetbrains.com/pycharm/)
- **选择**: Community 版本（免费）

### 学习文件夹结构

```
深度学习学习/
├── 01_基础概念/
│   ├── 01_张量操作.ipynb
│   ├── 02_自动微分.ipynb
│   └── 03_数据加载.ipynb
├── 02_神经网络/
│   ├── 01_全连接网络.ipynb
│   ├── 02_卷积网络.ipynb
│   └── 03_循环网络.ipynb
├── 03_实战项目/
│   ├── 手写数字识别/
│   ├── 猫狗分类/
│   └── 文本情感分析/
└── datasets/
    ├── 练习数据/
    └── 项目数据/
```

---

## 🚀 快速开始模板

创建 `learning_template.py`：

```python
# learning_template.py - 学习模板

import torch
import torch.nn as nn
import torch.optim as optim
import numpy as np
import matplotlib.pyplot as plt

# 设置中文字体（可选）
plt.rcParams['font.sans-serif'] = ['SimHei']

class SimpleNet(nn.Module):
    """一个简单的神经网络"""
    def __init__(self):
        super(SimpleNet, self).__init__()
        self.fc1 = nn.Linear(10, 5)  # 10个输入特征，5个隐藏单元
        self.fc2 = nn.Linear(5, 1)   # 5个隐藏单元，1个输出
    
    def forward(self, x):
        x = torch.relu(self.fc1(x))
        x = self.fc2(x)
        return x

def test_basic_operations():
    """测试基本操作"""
    print("🧪 测试基本张量操作...")
    
    # 创建张量
    x = torch.tensor([[1, 2], [3, 4]])
    y = torch.ones(2, 2)
    
    print(f"张量 x:\n{x}")
    print(f"张量 y:\n{y}")
    print(f"x + y:\n{x + y}")
    print(f"x * y:\n{x * y}")

def test_model():
    """测试神经网络模型"""
    print("\n🧠 测试神经网络...")
    
    # 检查设备
    device = torch.device('cuda' if torch.cuda.is_available() else 'cpu')
    print(f"使用设备: {device}")
    
    # 创建模型
    model = SimpleNet().to(device)
    print("神经网络创建成功！")
    
    # 测试随机数据
    x = torch.randn(5, 10).to(device)  # 5个样本，每个样本10个特征
    output = model(x)
    print(f"输入形状: {x.shape}")
    print(f"输出形状: {output.shape}")

def main():
    print("=" * 60)
    print("🚀 PyTorch 学习之旅开始！")
    print("=" * 60)
    
    test_basic_operations()
    test_model()
    
    print("\n" + "=" * 60)
    print("🎉 环境准备完成！开始你的深度学习之旅吧！")
    print("=" * 60)

if __name__ == "__main__":
    main()
```

---

## ❌ 常见问题解决

### 问题1：`pip` 不是内部或外部命令
**原因**: Python 没有正确安装或没有添加到环境变量

**解决**:
1. 重新安装 Python，安装时勾选 **"Add Python to PATH"**
2. 或者使用 Anaconda

### 问题2：下载速度慢
**解决**: 使用国内镜像

```bash
# 使用清华镜像
pip install torch torchvision torchaudio -i https://pypi.tuna.tsinghua.edu.cn/simple/

# 或者使用阿里云镜像
pip install torch torchvision torchaudio -i https://mirrors.aliyun.com/pypi/simple/
```

### 问题3：内存不足
**解决**:
```python
import torch
torch.set_num_threads(2)  # 限制线程数

# 清理内存
torch.cuda.empty_cache()  # 如果有GPU的话
```

### 问题4：导入错误
**解决**:
```bash
# 重新安装
pip uninstall torch torchvision torchaudio
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
```

---

## 📅 第一周学习计划

| 天数 | 学习内容 | 实践项目 |
|-----|---------|----------|
| **Day 1** | 环境配置 + 基础概念 | 运行测试脚本 |
| **Day 2** | 张量操作 | 创建各种张量，进行数学运算 |
| **Day 3** | 自动微分 | 实现简单函数的梯度计算 |
| **Day 4** | 神经网络基础 | 创建第一个神经网络 |
| **Day 5** | 数据加载 | 加载和处理数据集 |
| **Day 6** | 训练流程 | 完成第一个模型训练 |
| **Day 7** | 复习巩固 | 制作学习笔记 |

---

## 🎓 学习资源推荐

### 官方资源
- **[PyTorch 官方教程](https://pytorch.org/tutorials/)** - 最权威的学习资料
- **[PyTorch 文档](https://pytorch.org/docs/stable/index.html)** - API查询

### 中文资源
- **《动手学深度学习》** - 李沐等人的经典教材
- **B站视频教程** - 搜索 "PyTorch 入门"

### 练习平台
- **Kaggle Learn** - 免费的深度学习课程
- **LeetCode** - 算法练习

---
