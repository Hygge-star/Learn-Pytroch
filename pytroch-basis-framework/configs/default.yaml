data:
  dataset: "CIFAR10"
  batch_size: 64
  num_workers: 4

model:
  name: "BasicCNN"
  num_classes: 10

training:
  epochs: 50
  learning_rate: 0.001
  optimizer: "Adam"
  scheduler: "StepLR"

logging:
  log_dir: "./logs"
  save_interval: 5
